# 前端对后端的修改建议清单（feat-front-large-edit 分支提出）

> 本文件由前端同事 xiaojiangxishi 在 feat-front-large-edit 分支提出，记录前端所需但后端缺失的接口。
>
> **2026-07-18 复核修正**：上一版处理状态标注（2026-07-16）有多处不实——声称已实现的
> #15/#17/#19/#20/库存流水对应的 commit（80e71aa / 22d266a / d430af3 / fed8b31 / 891649c）
> 在 git 历史中**均不存在**，代码里当时也确实没有对应接口（属"计划被误标成已实现"）。
> 本次逐条按当前代码真实状态重新标注，并把缺失接口补齐：
>
> - #7 预约列表 patientName —— ✅ 已实现（`AppointmentDTO.ListItem` 含 patientName）。前端 ReceptionList.vue 用 `row.patientName || row.patientNo` 兜底。
> - #15 科室增改删 —— ✅ **2026-07-17 补齐**：POST/PUT/DELETE /departments（commit `d6e3567`），departmentNo 后端生成，删除前校验关联启用医生。
> - #16 默认排班 —— ✅ **2026-07-18 补齐**：新建 `doctor_schedule_template` 表（周几出诊规律模板，独立于 doctor_schedule）+ `/api/v1/schedule-templates` CRUD + `POST /apply` 一键生成（commit `5d48a3a`）。前端新增「排班模板」菜单页（ScheduleTemplate.vue）。决策：按周几(1-7)定义、起始日期+周数(1-8)生成、已存在跳过（幂等）。
> - #17 排班 ListItem 补字段 + PUT 全量更新 —— ⚠️→✅ ListItem 字段早已补；PUT /schedules/{id} **2026-07-18 补齐**（commit `accccd3`），前端 updateScheduleApi 同步从 PATCH /status 兜底改为 PUT。
> - #19 修改密码 —— ✅ **2026-07-18 补齐**：POST /auth/change-password（commit `456a193`），含频率限制（5分钟5次防爆破）。
> - #20/#21 管理员创建用户 —— ✅ **2026-07-17 补齐**：POST /auth/users（commit `d6e3567`），支持管理类角色；DELETE /auth/users/{id} 一并补齐。
> - 全局库存流水（drug.js getStockFlowListApi）—— ✅ **2026-07-18 补齐**：GET /drugs/stock/flows（commit `2e6c25c`），仅 PHARMACY_ADMIN/HOSPITAL_ADMIN 可访问。
>
> 附：前端 DoctorManage（医生增改删）、UserManage/PharmacyAdminManage（用户删除）此前调用的
> system.js 假成功占位——上一版称"已改为前端明确拒绝(commit d1cd4fd)"**不实**（实际仍是假成功）。
> 2026-07-17 已补齐全部对应后端接口并把前端占位改为真实对接（commit `d6e3567`）。
>
> **结论：本清单全部接口（#7/#15/#16/#17/#19/#20/#21 + 全局库存流水）均已实现。** ✅

---

7.后端预约列表接口（GET /appointments）的 ListItem 只返回 patientNo（患者编号），不返回 patientName（患者姓名）。需要修改。





15.新增科室，编辑科室，删除科室接口

POST /departments — 新增科室接口（需要后端自动生成 department\_no 科室编号）

PATCH /departments/{departmentId} — 更新科室接口

DELETE /departments/{departmentId} — 删除科室接口





16.默认排班功能：如要增加默认排班功能，需要后端先设计默认排班及其接口。











17\.

已有的 GET /api/v1/schedules 排班列表接口，返回的排班列表项（ListItem）需要补充返回以下字段，否则前端编辑时无法自动回填信息：

scheduleId/id：排班主键ID（用于编辑/停诊/删除接口的路径参数）

doctorId：排班关联的医生ID

departmentId：排班关联的科室ID

\---------------------------------------------------------------------------------------------------------------



目前只有PATCH /api/v1/schedules/{id}/status接口，仅支持修改排班状态（停诊/恢复），需要新增全量更新排班接口：



接口路径：PUT /api/v1/schedules/{scheduleId}

权限：仅HOSPITAL\_ADMIN角色可访问

请求参数（和创建排班接口字段一致）：

{

&#x20; "doctorId": "医生ID",

&#x20; "scheduleDate": "排班日期（YYYY-MM-DD）",

&#x20; "period": "时段：MORNING/AFTERNOON",

&#x20; "startTime": "开始时间（HH:mm）",

&#x20; "endTime": "结束时间（HH:mm）",

&#x20; "totalQuota": 总号源数（数字）,

&#x20; "registrationFee": 挂号费（数字，单位元）

}







19\.

实现 POST /auth/change-password 接口，处理密码修改逻辑。

接口路径：POST /api/v1/auth/change-password

请求参数：{ oldPassword: "原密码", newPassword: "新密码（需8-64位，至少含字母和数字）" }







20，21

缺少管理员创建用户的专用接口 现有POST /api/v1/auth/register是自助注册接口，仅允许创建PATIENT/DOCTOR角色，前端医院管理员后台新增医院管理员/药房管理员时调用该接口会被直接拦截报错，无法创建管理类角色用户。 需要新增已登录管理员专用的创建用户接口，支持创建所有角色（PATIENT/DOCTOR/PHARMACY\_ADMIN/HOSPITAL\_ADMIN），请求参数和现有register一致（account/name/phone/password/role/status），创建用户后需要同步在Redis写入角色key（复用现有register写角色的逻辑即可），否则登录后角色会被兜底为PATIENT导致跳转异常。













