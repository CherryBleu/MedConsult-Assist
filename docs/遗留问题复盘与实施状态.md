# 架构遗留问题复盘与实施状态

> 本文件记录 fullstack-review 第 1–6 轮收尾时识别的 5 项架构遗留问题：现状证据、未在前期被发现的原因复盘、以及目标实施策略。
>
> 适用范围：作为后续分批实施的基线，所有 `file:line` 引用以本文落盘时（2026-07-16）的代码为准。

## 零、2026-07-19 当前状态校准（15 项总览）

本节是 2026-07-19 截至当前分支 HEAD `ec3cd5f` 对源码、已提交前端调用、最近提交和本地运行依赖复核后的当前事实，保留后文 2026-07-16 段落作为历史快照。状态同步到 `docs/修改建议.md` 第一节；“代码已实现”和“运行依赖已就绪”分开判定。A2/B2/C2/E2 的本轮并行结果若只处于 dry-run、复验或待主窗口汇总状态，不在本节提前关闭为“已实现”。

| # | 当前状态 | 核心证据 | 后续动作 |
| --- | --- | --- | --- |
| 1 处方链路 | 部分实现 | 后端已有 prescription/prescription_item 和 9 个总端点；处方详情已允许 PATIENT 本人访问，详情与 pay service 已按处方 patient_id 校验 owner；2026-07-19 已补患者 `/patient/prescriptions` 列表、详情抽屉、缴费对话框、mock 和移动端/错误恢复 E2E；药师 `/pharmacy/prescription-review` 已补移动明细卡片、审方失败恢复和 44px 操作目标；医生 `/doctor/record/write` 已补结构化处方明细、创建处方并提交审方前端流程 | 评估调剂异步化，并跑真实后端 E2E |
| 2 审计 MQ | 已实现 | `audit_log`、AuditLogConsumer 和查询/内写接口已存在；`b5b37ad` 为 AI 消费端补有界重试和 confirm 后 DLQ，并将 AI/通知共用幂等状态改为 `PROCESSING/DONE`；common-mq 已提供共享 `AuditLogEvent`、`@AuditLog` AOP 与 `AuditLogProducer`，patient-service 患者建档/更新/状态变更、outpatient-service 预约创建/支付/签到/就诊状态变更/取消、auth-service 登录成功/改密/补建档绑定、medical-record-service 病历/附件/处方关键写操作、drug-service 药品/库存关键写操作已写入事务内 `local_message` audit outbox；ai-service 影像任务提交已写入事务内 `local_message` imaging outbox | 新增业务写接口继续按治理清单补审计标注和 outbox 验证 |
| 3 RBAC | 部分实现 | `backend/auth-service/src/main/resources/schema.sql:80,107,120` 建 RBAC 表；`AuthServiceImpl.resolveScope` 已用过渡期角色权限点替代用户 JWT `scope=*`，登录/补建档重签/刷新链路均覆盖；角色仍读 Redis，用户分页仍有 Redis O(U) 角色回填 | 用 RBAC 表驱动 token scope、用户分页和角色回填 |
| 4 internal Feign | 部分实现 | common-feign 客户端和 `/internal/**` 已落地；2026-07-19 已修复服务头伪造身份、用户触发内部 Feign 误走 user JWT、service-token bootstrap 递归和默认 provider 为空的问题；本轮补强 common-security：`/internal/**` 不再先吃 `X-User-*` 网关头，混入用户头时仍只用 SERVICE JWT 建身份；业务/AI internal 端点已改为 `requireService(scope)` 逐项强制服务权限点；auth-service 已补 `verify`、`service-verify`、`users/{userId}/roles` 内部端点 | 建逐端点内部契约矩阵，继续确认生产态服务 token 换发策略 |
| 5 症状自诊 | 暂缓 | 代码已有规则优先、Mongo/Milvus 检索、证据展示与 readiness；疾病缓存键已纳入 `medconsult.ai.rag.knowledge-version` / `RAG_KNOWLEDGE_VERSION`；运行态 Mongo diseases=8807、Embedding=512 维，Milvus `medical.data` 已导入并可查询 8807 个源 JSON 主键；已补 `GET /api/v1/ai/rag/probes` 固定症状探针接口和单元门禁；2026-07-19 真实依赖环境探针 3/3 通过；三张规则表为空，仅硬编码 fallback 可用 | 2026-07-19 用户确认规则种子、离线 Recall@K、引用正确性评测、可追溯 LLM 标准化暂时放弃 |
| 6 AI 影像拆表 | 已实现 | `schema-ai.sql:83,108` 拆为 ai_report_text_analysis / ai_image_detection；`AiReportTextAnalysisEntity.java:8`、`AiImageDetectionEntity.java:8` 分别映射；数据库设计、架构文档、接口文档、README 与 Postman 样例已统一为 `imaging-detection` 路径 | 关闭，后续只随真实接口演进同步维护 |
| 7 服务账号 | 部分实现 | `auth-service/schema.sql:38` 建 sys_service_account；`ServiceAccountInternalController.java:39` 可换发服务 token；2026-07-19 `MedConsultFeignAutoConfiguration` 默认 provider 已可在存在 `JwtCodec` 时为当前服务签发 SERVICE JWT，auth/ai 仍可用自定义 provider 覆盖；业务/AI internal 端点已按 SERVICE JWT scope 校验；auth-service 内部 service-verify 和 roles 查询已校验服务账号 ACTIVE 与 token scope 不超授权 | 继续补跨服务覆盖矩阵，明确生产态是否统一使用 auth-service API Key 换发 |
| 8 技术栈能力 | 部分实现 | `AiController.java` 有 3 个 POST SSE，`8b53367` 收紧 SSE 断开/完成生命周期；Redis 锁用于预约/处方/库存；`b5b37ad` 补 AI 队列重试、幂等落标、DLQ 与 confirm；ai-service 影像提交已改为事务 `local_message` outbox；2026-07-19 病历摘要页已补 fetch + ReadableStream 消费 `/ai/summary/stream`，mock 流覆盖 start/delta/result/done/error，E2E 覆盖流式状态、错误重试、移动端无横向滚动和 44px 触控 | 补 SSE 完整事件/错误/重连契约，并将分诊/用药分析前端也切到 stream 消费 |
| 9 字段一致性 | 部分实现 | `docs/数据库设计文档.md:42` 仍写 sys_user.role；`SysUser.java:13` 已移到 sys_user_role；数据库文档仍有病历内嵌处方描述；AI 影像旧混表描述已改为 `ai_report_text_analysis` / `ai_image_detection` 分表 | 生成字段/枚举/表映射清单，逐项清零 |
| 10 文件上传 | 部分实现 | `802dd48` 已补动态 presigned TTL、JWT owner 与 canonical `minio://`；`ac706bc` 将影像提交改为逐个授权 `fileIds`，校验 patient/record 上下文、最多 8 张和流式大小，并按患者/提交医生/serviceCode/管理员限制任务读取 | 执行影像安全迁移维护窗口验收，补运行态孤儿对象对账，并将医生 `ASSIGNED` 接诊范围下沉到数据查询 |
| 11 就诊闭环 | 部分实现 | 患者 SELF 签到已补 `POST /appointments/{appointmentId}/check-in`，且未支付签到返回 409；医生 `PATCH /appointments/{appointmentId}/status` 保持 DOCTOR-only；2026-07-19 患者选择科室页已补语义按钮、移动端无横向滚动和错误重试 E2E；需求仍要求叫号/过号/复诊关联 | 补叫号/过号、复诊关联和端到端验收 |
| 12 退费/检查检验 | 部分实现 | FEFO 已做；退款已补 `refund_order`、`POST /appointments/{id}/refund`、RefundService/Controller、患者端入口与 E2E；payment patch 已禁止伪造 REFUNDING/REFUNDED；全仓仍无检查检验域 | 检查检验进入产品决策；真实支付渠道接入时补回调/对账/失败重试 |
| 13 敏感数据/行级权限 | 未实现 | `patient-service/schema.sql:15-18` id_no 仍明文 TODO；`PatientServiceImpl.java:79,155` 明文 eq/like；`Permission.java:15,38` dataScope 只声明 | 建 common-crypto、盲索引迁移和 SELF/ASSIGNED SQL 过滤 |
| 14 规范一致性 | 部分实现 | `docs/接口文档.md:11` 默认 pageSize=10，但 `:224` 等处仍为 20；GET 请求体示例仍残留；前端 page/pageNum 与本地分页混用 | 统一分页默认值和 GET 示例，矩阵校验 |
| 15 入口/越权 | 部分实现 | 入口校验、纯管理员 STAFF 入口、未知 clientType 拒绝、写接口 @Permission 和患者 SELF 签到已补；文件/影像最小 owner 边界、JWT 复核人、跨患者/病历挂接拒绝已有测试；处方详情/缴费 owner 已补并覆盖他人患者拒绝测试；用户 JWT `scope=*` 已收口为角色权限点映射；通用 SELF/ASSIGNED 尚未下沉 SQL | 推进 SELF/ASSIGNED SQL，并补医生接诊范围 |

### 0.1 2026-07-19 最新执行回写

> 2026-07-19 起，前端治理记录口径调整为：工作人员端/管理端页面优先记录桌面浏览器缩放和较窄桌面宽度稳定性；关闭关键词为页面无横向溢出、宽表由容器横向滚动、弹窗不挤出视口、错误态 `role="alert"` + 重试恢复。移动卡片和 44px 触控目标作为保留能力，不再作为本轮主要验收口径；覆盖率和 AI/RAG 评测本轮暂停。
>
> `ec3cd5f docs(frontend): 同步管理端桌面缩放验收口径` 是当前文档裁定口径；后续并行代理的新结果需以主窗口验证、提交或既有文档证据为准再回写。

- `86fe7c5 test(ai-service): 启用覆盖率门禁`：`ai-service` 已在模块 POM 启用 JaCoCo `report` + `check`，门禁为 LINE >= 90%、BRANCH >= 80%。首次门禁验证 `mvn -f backend/pom.xml -pl :ai-service -am verify` 通过，Tests run=377，Failures=0，Errors=0，Skipped=0，JaCoCo 输出 `All coverage checks have been met.`；后续当前分支相关 verify 已扩到 383 项并继续满足门禁。该事实关闭的是测试质量门禁，不代表 RAG 运行就绪或业务闭环关闭。
- `64fdf89 fix(vscode): 校准微服务启动工作目录`、`013cf1d fix(dev): 收紧VS Code微服务启动配置`、`8e4dd1e test(vscode): 增加启动配置回归校验`：`.vscode/launch.json` 的 8 个启动项仍只包含可部署微服务，`medconsult-common` 与 `data` 未配置为生产启动服务；各启动项 `cwd`、Maven `projectName` 和 `classPaths: ["$Runtime"]` 显式声明，且不引用不存在的必需 `envFile`。验证口径为 JSON 可解析、8 个 `cwd` 均存在、common/data 启动项数量为 0、common 测试锚点无 `main()`；`scripts/review/verify_vscode_launch.py` 当前由 A2 负责，本文只同步已提交状态。
- `32015bd fix(auth-service): 收口用户令牌权限范围`：登录、补建患者档案后重签、refresh 均不再签发 `scope=*`，改用 `PATIENT/DOCTOR/PHARMACY_ADMIN/HOSPITAL_ADMIN` 过渡期角色权限点映射；refresh 会将历史 `scope=*` refresh token 改签为当前角色权限点；新增覆盖四类内置角色、refresh scope 保持、历史 wildcard refresh 重写、bind-patient 重签 scope 的集成测试。验证：新增 4 项 scope 测试通过，后续仍需 RBAC 五表驱动 scope 与 SELF/ASSIGNED SQL 下沉。
- `8a9c8bb fix(ai-service): 纳入RAG知识版本缓存维度`：`medconsult.ai.rag.knowledge-version` / `RAG_KNOWLEDGE_VERSION` 进入疾病缓存键身份哈希，默认值为 `medical-data-unified-8807-v1`；同一检索配置下仅知识内容版本变化也会生成不同 Redis key。验证：`mvn -f backend/pom.xml -pl :ai-service -am "-Dtest=DiseaseCacheServiceTest,RagReadinessServiceTest,MilvusRestClientTest" "-Dsurefire.failIfNoSpecifiedTests=false" test` 通过，32 项测试通过。
- 2026-07-19 RAG 运行依赖验收：Mongo `medconsult.diseases` 为 8807 条，样本中文字段正常；Embedding `http://localhost:7997/health` 返回 `dimension=512`、`model=BAAI/bge-small-zh-v1.5`；`backend/data` 导入器读取 8807 条、跳过 0 条、写入 8807 条到 Milvus `medical.data`。本地 Milvus 2.4.13 REST v2 不支持 `/v2/vectordb/collections/flush`，已用 `pymilvus` 执行 flush；REST `get_stats` 当前 `rowCount=8817`，但 `count(*)=8807` 且源 JSON 8807 个唯一 `_id` 与 Milvus 可查询 `id` 完全一致。后续 RAG 关闭项不再是“库/集合不存在”，而是固定症状探针、规则种子和召回质量门禁。
- 2026-07-19 患者 AI 问诊前端重构：`AiConsult.vue` 已从普通聊天卡片调整为临床问诊工作台，AI 回答先展示安全摘要，高风险摘要使用 `role="alert"`；RAG 证据改为 citation 卡片与向量 disclosure；失败消息保留原症状并提供重试。mock 覆盖“胸痛大汗怎么办”高风险回复和一次性发送失败；`symptom-chat.spec.ts` 覆盖移动端无横向滚动、`.ai-consult-action` >= 44px、键盘 disclosure 和 axe serious/critical 门禁。验证：`npm --prefix frontend run test:e2e -- symptom-chat.spec.ts` 6/6 通过；`npm --prefix frontend run build` 通过。这关闭的是患者 AI 问诊页体验缺口，不代表医生/药房工作台、处方前端闭环或真实后端 E2E 已完成。
- 2026-07-19 患者处方前端闭环：新增患者导航 `/patient/prescriptions`、处方列表/详情抽屉/缴费对话框、处方 API 字段归一化与 `VITE_USE_MOCK=true` mock；移动端卡片动作保持 44px 触控目标，列表失败与缴费失败均有 `role="alert"` 和重试路径。验证：`npm --prefix frontend run build` 通过；`npm --prefix frontend run test:e2e -- patient-prescriptions.spec.ts` 3/3 通过。该提交关闭患者处方列表/详情/缴费前端缺口，医生结构化开方与调剂异步化仍为遗留项。
- 2026-07-19 药师处方审核前端体验：`PrescriptionReview.vue` 已补处方详情移动端明细卡片、按处方编号命名的审方/发药操作、审核/发药对话框内 `role="alert"` 错误恢复和移动端 footer 布局；mock 增加审方/发药失败与列表旧响应延迟开关，`responsive-table.spec.ts` 改为在 `VITE_USE_MOCK=true` 下验证移动明细、44px 触控、审方失败恢复和旧响应不覆盖新筛选。验证：`npm --prefix frontend run build` 通过；`npm --prefix frontend run test:e2e -- responsive-table.spec.ts` 12/12 通过。该提交不关闭医生结构化开方或调剂异步化。
- 2026-07-19 药师库存管理前端体验：`StockManage.vue` 已补库存入库/出库按钮的具体 aria-label、移动端 44px 触控与 `touch-action`，库存列表和库存操作 mock 增加一次性失败开关；对话框内失败继续用 `role="alert"` 展示且保留输入。验证：新增 `pharmacy-stock.spec.ts` 红灯先因库存列表/操作失败态无 alert 失败；实现后 `npm --prefix frontend run test:e2e -- pharmacy-stock.spec.ts` 3/3 通过。该提交只关闭药师库存管理页的移动端卡片、筛选可用性和库存操作失败恢复缺口，不代表调剂异步化或真实后端 E2E 已完成。
- 2026-07-19 药师库存流水前端体验：`StockFlow.vue` 已补药品/类型筛选的可访问标签、移动端卡片列表、44px 查询/重置/分页触控目标、`touch-action` 和可见焦点；mock 增加 `mock_stock_flow_fail_once`，加载失败经 `PageState` 以 `role="alert"` 暴露并保留重试入口。验证：新增 `pharmacy-stock-flow.spec.ts` 红灯先因库存流水失败态无 alert 失败；实现后 `npm --prefix frontend run test:e2e -- pharmacy-stock-flow.spec.ts` 2/2 通过。该提交只关闭药师库存流水页的移动端筛选、卡片列表和失败恢复缺口，不代表调剂异步化或真实后端 E2E 已完成。
- 2026-07-19 药师药品目录前端体验：`DrugList.vue` 已补搜索框和分类/单位选择的可访问标签、表格/移动卡片操作的具体 aria-label、44px 操作目标、`touch-action` 和可见焦点；mock 增加 `mock_drug_list_fail_once` 与 `mock_drug_save_fail_once`，列表加载失败经 `PageState` 以 `role="alert"` 暴露并可重试，保存失败在对话框内 `role="alert"` 展示且保留输入。验证：新增 `pharmacy-drug-list.spec.ts` 红灯先因搜索框缺少 aria label、列表失败开关未生效、分类选择器定位不稳定失败；实现后 `npm --prefix frontend run test:e2e -- pharmacy-drug-list.spec.ts` 3/3 通过。该提交只关闭药师药品目录页的移动端搜索、卡片操作和失败恢复缺口，不代表药品真实后端 E2E、药品域完整治理或调剂异步化已完成。
- 2026-07-19 药房工作台前端体验：`PharmacyWorkbench.vue` 已将待处理预警条目标记为移动端可识别卡片，立即处理按钮补按药品名命名的 aria-label，工作台查看全部/处理按钮补 44px、`touch-action` 和可见焦点，预警文案支持长文本换行；已有 `PageState` 继续承担库存预警加载失败 `role="alert"` 和重试。验证：新增 `pharmacy-workbench.spec.ts` 红灯先因缺少 `pharmacy-workbench-warning-item` 失败；实现后 `npm --prefix frontend run test:e2e -- pharmacy-workbench.spec.ts` 2/2 通过。该提交只关闭药房工作台移动入口、预警处理按钮语义和失败恢复回归，不代表库存/药品真实后端 E2E 或调剂异步化已完成。
- 2026-07-19 医生排班前端体验：`MySchedule.vue` 已将日期切换从 `div @click` 改为语义 button，补 `aria-pressed`、可见焦点、移动端 44px 日期触控目标、排班卡片换行布局，并接入 `PageState` 加载/空/错误/重试状态；mock 增加 `mock_doctor_schedule_fail_once` 用于失败恢复回归。验证：新增 `doctor-schedule.spec.ts` 红灯先因日期标签不是 button、失败态无 `role="alert"` 失败；实现后 `npm --prefix frontend run test:e2e -- doctor-schedule.spec.ts` 2/2 通过。该提交只关闭医生排班页的移动端日期操作和错误恢复缺口，不代表排班管理域或真实后端 E2E 已完成。
- 2026-07-19 医生病历管理前端体验：`RecordList.vue` 已接入 `PageState` 加载/空/错误/重试状态，病历条目调整为移动端稳定卡片，查看/继续编辑按钮补具体 aria-label、44px 触控目标和可见焦点；mock 增加 `mock_doctor_record_list_fail_once` 用于医生侧失败恢复回归。验证：新增 `doctor-records.spec.ts` 红灯先因缺少移动卡片和失败态 `role="alert"` 失败；实现后 `npm --prefix frontend run test:e2e -- doctor-records.spec.ts` 2/2 通过。该提交只关闭医生病历管理列表的移动端和错误恢复缺口，不代表病历真实后端 E2E 或整个病历域已闭环。
- 2026-07-19 医生结构化开方前端：`RecordWrite.vue` 将处方从病历文本框改为结构化药品明细，前端提交时先保存/更新病历草稿，再走 `POST /prescriptions` 创建处方并 `POST /prescriptions/{id}/submit` 提交审方；AI 用药分析改读结构化处方项，提交失败在处方区域内 `role="alert"` 展示且保留输入。验证：新增 e2e 红灯先因缺少 `structured-prescription-item` 失败；实现后 `npm --prefix frontend run test:e2e -- prescription.spec.ts` 3/3 通过。该提交关闭医生开方/提交审方的 mock 前端缺口，不代表调剂异步化或真实后端 E2E 已完成。
- 2026-07-19 医生用药分析前端体验：`MedicationAnalysis.vue` 已补病历/药品选择器稳定类、移动端 44px 选择器和开始分析按钮、`touch-action` 与可见焦点，分析结果区增加稳定测试标识；mock 增加 `mock_medication_analysis_fail_once`，分析失败经 `PageState` 以 `role="alert"` 暴露并可重试。验证：新增 `doctor-medication-analysis.spec.ts` 红灯先因缺少稳定选择器失败，随后因选择器触控高度只有 32px 失败；实现后 `npm --prefix frontend run test:e2e -- doctor-medication-analysis.spec.ts` 2/2 通过。该提交只关闭医生用药分析页的移动表单、结果展示和失败恢复回归，不代表 AI/RAG 质量评测、可追溯 LLM 标准化或真实后端 E2E 已完成。
- 2026-07-19 患者影像检测前端体验：`ImagingDetection.vue` 已补患者影像历史移动端卡片、`PageState` 加载/空/失败/重试、上传预览和检测结果图的动态 alt、canvas 标注对应的异常区域文字说明、上传/轮询失败 `role="alert"` 错误恢复和 44px 操作目标。验证：新增 `patient-imaging.spec.ts` 红灯先因缺少 `responsive-patient-imaging-card` 和预览图 alt 失败；实现后 `npm --prefix frontend run test:e2e -- patient-imaging.spec.ts` 2/2 通过。该提交关闭患者影像页的替代文本和移动历史体验缺口，不代表影像真实后端 E2E 或医生 `ASSIGNED` 范围已完成。
- 2026-07-19 医生影像辅助前端体验：`ImagingAssist.vue` 已补医生影像任务列表移动端卡片回归、上传错误 `role="alert"` 恢复路径、预览移除按钮 44px 触控目标与 canvas 装饰层语义隐藏。验证：新增 `doctor-imaging-assist.spec.ts` 红灯先因预览移除按钮只有 24px 失败；实现后 `npm --prefix frontend run test:e2e -- doctor-imaging-assist.spec.ts` 2/2 通过。该提交只关闭医生影像辅助页的移动任务卡片、上传错误恢复和预览操作触控缺口，不代表影像真实后端 E2E 或医生 `ASSIGNED` 数据范围已完成。
- 2026-07-19 医生工作台移动端体验：`DoctorWorkbench.vue` 已将快捷入口从 `div @click` 改为语义 button，补 `PageState` 加载/空/错误/重试状态、移动端内容列布局、44px 工作台操作目标和可见 focus 样式。验证：新增 `doctor-workbench.spec.ts` 红灯先因“接诊管理”快捷入口不是 button 失败；实现后 `npm --prefix frontend run test:e2e -- doctor-workbench.spec.ts` 1/1 通过。该提交只关闭医生工作台移动快捷入口和键盘可访问性缺口。
- `aab8c4b feat(frontend): 优化管理员医生管理桌面缩放体验`：此前 `DoctorManage.vue` 已接入 `PageState` 和 `ResponsiveTable`，移动端医生信息卡片、44px 操作目标、具体 aria-label 与列表失败恢复作为历史事实保留；本轮改为桌面缩放验收口径，代码改动限于 `frontend/src/views/admin/system/DoctorManage.vue` 和 `frontend/e2e/admin-doctor-manage.spec.ts`，mock 未修改。1024/1280 桌面宽度下页面级无横向溢出，宽表由 `admin-doctor-table-shell` 容器承接横向滚动，新增医生弹窗在 1024x620 不越界并在内部滚动，列表加载失败以 `role="alert"` 暴露且可重试恢复。验证：生产代码修改前 `npm --prefix frontend run test:e2e -- admin-doctor-manage.spec.ts` 4 failed，缺少 `admin-doctor-table-shell` 且弹窗底部超出 1024x620 视口；子代理 B 实现后同命令 4/4 通过；`git diff --check -- frontend/src/views/admin/system/DoctorManage.vue frontend/e2e/admin-doctor-manage.spec.ts` exit 0，仅 CRLF 提示；子代理 B 当时未跑 build。主窗口最终统一运行 `npm --prefix frontend run test:e2e -- admin-doctor-manage.spec.ts admin-schedule-manage.spec.ts`，结果 8 passed（医生 4/4、排班 4/4），并运行 `npm --prefix frontend run build` 通过。该提交只关闭管理员医生管理页的桌面缩放、宽表滚动容器、弹窗视口约束和错误恢复缺口，不代表医生新增/更新/删除真实后端接口、全量 E2E、真实后端 E2E、覆盖率或 AI/RAG 评测完成。
- `bdfe76d feat(frontend): 优化管理员排班管理桌面缩放体验`：此前 `ScheduleManage.vue` 已接入 `PageState` 和 `ResponsiveTable`，移动端排班信息卡片、44px 操作目标、`touch-action`、具体 aria-label 与列表失败恢复作为历史事实保留；本轮改为桌面缩放验收口径，代码改动限于 `frontend/src/views/admin/schedule/ScheduleManage.vue` 和 `frontend/e2e/admin-schedule-manage.spec.ts`，mock 未修改。1024/1280 较窄桌面宽度下筛选栏、宽表、分页、批量操作、新增/批量弹窗 footer 不造成页面级横向溢出；宽表由容器横向滚动承接；弹窗保持视口安全边距，body 内部滚动；列表加载失败以 `role="alert"` 暴露且可重试恢复。验证：生产代码修改前 `npm --prefix frontend run test:e2e -- admin-schedule-manage.spec.ts` 4 failed，桌面表格容器 `overflow-x: visible`、弹窗 body `overflow-y: visible`；子代理 C 实现后同命令 4/4 通过，`npm --prefix frontend run build` 通过。主窗口最终统一运行 `npm --prefix frontend run test:e2e -- admin-doctor-manage.spec.ts admin-schedule-manage.spec.ts`，结果 8 passed（医生 4/4、排班 4/4），并运行 `npm --prefix frontend run build` 通过。该提交只关闭管理员排班管理页的桌面缩放、宽表滚动容器、弹窗视口约束和错误恢复缺口，不代表默认排班模型、全量 E2E、真实后端 E2E、覆盖率或 AI/RAG 评测完成。
- `0518993 feat(frontend): 优化管理员科室管理桌面缩放体验`：`DepartmentManage.vue` 接入 `PageState` 和桌面表格 shell，科室宽表在 1024px/桌面缩放等效宽度下由表格容器承接横向滚动，页面不产生横向溢出；新增/编辑弹窗使用视口约束宽度并保留安全边距；科室列表加载失败通过 `role="alert"` 暴露并可重试恢复。验证：新增 `admin-department-manage.spec.ts` 红灯先因缺少 `admin-department-table-shell`、弹窗测量对象不正确失败；实现后 `npm --prefix frontend run test:e2e -- admin-department-manage.spec.ts` 3/3 通过，`npm --prefix frontend run build` 通过。该提交只关闭管理员科室管理页桌面缩放、横向滚动容器、弹窗不溢出和错误恢复缺口，不代表后端科室接口、真实后端 E2E、覆盖率或 AI/RAG 评测已完成。
- 2026-07-19 患者选择科室前端体验：`DepartmentList.vue` 已将科室卡片和智能分诊折叠头改为语义 button，科室列表接入 `PageState` 加载/空/错误/重试状态，移动端改为单列卡片并保持 44px 以上操作目标；mock 增加 `mock_department_list_fail_once` 用于失败恢复回归。验证：新增 `appointment.spec.ts` 红灯先因科室卡片不是 button、失败态无 `role="alert"` 失败；实现后 `npm --prefix frontend run test:e2e -- appointment.spec.ts` 3/3 通过，`npm --prefix frontend run build` 通过；375px/1440px 截图和宽度指标确认无横向溢出。该提交只关闭患者挂号入口的移动端与键盘可访问性缺口，叫号/过号/复诊仍为遗留项。
- 2026-07-19 患者病历列表前端体验：`RecordList.vue` 已将病历卡片从 `div @click` 改为语义 button，接入 `PageState` 加载/空/错误/重试状态，移动端头尾信息改为可换行列布局并保持 44px 以上操作目标；mock 增加 `mock_patient_record_list_fail_once` 用于失败恢复回归。验证：新增 `patient-records.spec.ts` 红灯先因病历卡片不是 button、失败态无 `role="alert"` 失败；实现后 `npm --prefix frontend run test:e2e -- patient-records.spec.ts` 3/3 通过，`npm --prefix frontend run build` 通过，390px/1440px 宽度指标确认无横向溢出。该提交只关闭患者病历列表的移动端、键盘可访问性和错误恢复缺口，不代表病历真实后端 E2E 或整个病历域已闭环。
- `923c96c feat(ai-service): 增加RAG固定症状探针`：新增 `RagProbeService` 和 `GET /api/v1/ai/rag/probes`，固定检查呼吸道咳嗽、儿童鸡鸣样咳嗽和胸痛急症三类样本，结果包含召回数量、命中疾病、命中字段、风险等级、急症建议和失败 reasonCodes。验证：红灯先因 `RagProbeService` 缺失编译失败；实现后 `mvn -f backend/pom.xml -pl :ai-service -am "-Dtest=RagProbeServiceTest,AiControllerDelegationTest,AiControllerSummaryPermissionTest" "-Dsurefire.failIfNoSpecifiedTests=false" test` 通过，8 项测试通过。该提交只证明探针能力和接口契约，真实环境探针通过与规则种子导入仍待执行。
- `e702d56 fix(ai-service): 提升RAG固定探针召回质量`：`DiseaseSearchService` 本地规则新增儿童鸡鸣样咳嗽与胸痛急症疾病提示，Mongo 症状召回已满但命中高特异信号时仍补 Milvus 语义召回，并按疾病提示、症状证据和科室字段重排，避免通用咳嗽/胸痛结果挤掉百日咳和心血管急症。验证：先新增 3 个失败用例证明旧行为；修复后 `mvn -f backend/pom.xml -pl :ai-service -am "-Dtest=DiseaseSearchServiceTest,RagProbeServiceTest,RagReadinessServiceTest" "-Dsurefire.failIfNoSpecifiedTests=false" test` 通过 24 项；真实 Mongo/Embedding/Milvus 环境 readiness 全 UP（8807/8807/512），`GET /api/v1/ai/rag/probes` 返回 `passed=true`，RESPIRATORY_COUGH / PERTUSSIS_CHILD / CARDIAC_EMERGENCY 均为 UP。完整 `mvn -f backend/pom.xml -pl :ai-service -am verify` 通过 383 项，JaCoCo 输出 `All coverage checks have been met.`，LINE 4102/4421（92.78%）、BRANCH 1749/2177（80.34%）。
- `8ca4176 feat(mq): 接入患者档案审计outbox`：common-mq 新增共享 `AuditLogEvent`、`@AuditLog`、`AuditLogProducer` 与 AOP，生产端将审计事件写入 `local_message` 并走既有 MessageDispatcher 投递；notification-service 消费端改用共享事件 DTO。patient-service 已引入 common-mq、补 `local_message` DDL，并为患者建档、档案更新、状态变更写入 `audit.log` outbox。验证：先新增 common-mq producer/aspect 红灯测试与 patient-service outbox 红灯测试；修复后 `mvn -f backend/pom.xml -pl :common-mq -am "-Dtest=AuditLogOutboxTest" "-Dsurefire.failIfNoSpecifiedTests=false" test` 通过 3 项，`mvn -f backend/pom.xml -pl :patient-service -am "-Dtest=PatientFlowTest#patientMutations_enqueueAuditLogOutboxMessages" "-Dsurefire.failIfNoSpecifiedTests=false" test` 通过 1 项。
- `f5ecd66 feat(mq): 接入预约链路审计outbox`：outpatient-service 引入 common-mq、补 `local_message` DDL，并为预约创建、支付状态更新、患者签到、医生就诊状态变更和取消预约写入 `audit.log` outbox；common-mq 同步收紧审计 `message_no` 片段长度，保证 64 位列宽内可容纳 APPOINTMENT/STATUS_CHANGE 等长组合。验证：先新增 `OutpatientFlowTest#appointmentMutations_enqueueAuditLogOutboxMessages` 红灯，旧代码因 H2 缺 `local_message` 表失败；实现后又复现并修复 `message_no` 超长 65 位问题；最终 `mvn -f backend/pom.xml -pl :common-mq,:outpatient-service -am "-Dtest=AuditLogOutboxTest,OutpatientFlowTest" "-Dsurefire.failIfNoSpecifiedTests=false" test` 通过，common-mq 4 项、outpatient-service 9 项。
- `034f0aa feat(mq): 接入认证链路审计outbox`：auth-service 引入 common-mq、补 `local_message` DDL，并为登录成功、修改密码、补建档绑定写入 `audit.log` outbox；注册/管理员建号仍使用 `TransactionTemplate`，严格事务 outbox 建议单独重构。验证：先新增 `AuthFlowTest#authSecurityMutations_enqueueAuditLogOutboxMessages` 红灯，旧代码因 H2 缺 `local_message` 表失败；实现后该测试通过，确认 `LOGIN` 与 `PASSWORD_CHANGE` 两条 USER 审计消息入 outbox。
- `e92041c feat(mq): 接入病历处方审计outbox`：medical-record-service 引入 common-mq、补 `local_message` DDL，并为病历创建/更新/归档、附件元数据创建、处方创建/提交/审方/缴费/调剂/完成/取消写入 `audit.log` outbox；病历/处方 create 的 DB 写入已从 self-call `TransactionTemplate` 迁到独立事务 Bean，保证 `@AuditLog` 走代理。验证：先新增 `MedicalRecordFlowTest#clinicalDocumentMutations_enqueueAuditLogOutboxMessages` 红灯，旧代码因 H2 缺 `local_message` 表失败；实现后该测试通过，确认 14 条审计消息入 outbox。
- `aa6928e feat(mq): 接入药品库存审计outbox`：drug-service 引入 common-mq、补 `local_message` DDL，并为药品创建、库存入库、库存出库和出库回滚补偿写入 `audit.log` outbox。验证：先新增 `DrugFlowTest#drugMutations_enqueueAuditLogOutboxMessages` 红灯，旧代码因 H2 缺 `local_message` 表失败；实现后 `mvn -f backend/pom.xml -pl :common-mq,:drug-service -am "-Dtest=AuditLogOutboxTest,DrugFlowTest#drugMutations_enqueueAuditLogOutboxMessages" "-Dsurefire.failIfNoSpecifiedTests=false" test` 通过，确认 4 条审计消息入 outbox。
- `21f3cbb feat(mq): 影像任务提交接入事务outbox`：`ImagingDetectionService.submitImageDetection` 不再直接 `RabbitTemplate.convertAndSend`，而是在提交事务内写入 `local_message`，payload 为 `ImageDetectionTaskMessage(detectionId, traceId)` JSON，后续由 MessageDispatcher 投递到 `medconsult.ai.imaging` / `ai.image.detect`。验证：先将 `ImagingDetectionServiceTest` 改为 outbox 断言并复现构造签名红灯；实现后 `mvn -f backend/pom.xml -pl :ai-service -am "-Dtest=ImagingDetectionServiceTest" "-Dsurefire.failIfNoSpecifiedTests=false" test` 通过 31 项。
- `1c6bad2 fix(feign): 收紧内部服务身份契约`、`7749ac1 fix(security): 收紧内部接口身份解析优先级`：`JwtAuthServletFilter` 不再把 `X-Caller-Service`/`X-Service-Code` 当作服务认证来源，服务身份只来自已验签 SERVICE JWT；`AuthRelayInterceptor` 对 `/internal/**` 固定使用服务 token，即使当前请求有用户身份也不透传 `X-User-*` 作为内部认证头，并对 `/internal/auth/service-token` 跳过 Authorization 注入避免 provider 递归；`MedConsultFeignAutoConfiguration` 在存在 `JwtCodec` 时提供默认本地签发的 SERVICE JWT provider，auth/ai 自定义 provider 继续覆盖。本轮补强 `/internal/**` 解析优先级：即使请求混入 `X-User-*` 网关头，也跳过用户头分支并按 SERVICE JWT 建身份，避免 internal 服务身份被错误降级为 USER。验证：先新增 common-security/common-feign 红灯测试复现头伪造、JWT 被 header 覆盖、用户触发内部 Feign 走 user JWT、bootstrap 递归和默认 provider null；本轮新增 `JwtAuthServletFilterTest#internalPathIgnoresGatewayUserHeadersAndUsesServiceJwt` 先失败后通过；修复后 `mvn -f backend/pom.xml -pl :common-security,:common-feign -am test` 通过，受影响服务 `test-compile` 通过，聚焦内部接口 MockMvc 测试通过 patient 1、drug 2、medical-record 1、notification 3。
- `a6da0e9 fix(security): 强制内部服务权限点`：`SecurityContext` 新增 `requireService(permissionCode)`，先强制 subjectType=SERVICE，再按 JWT scope / `*` 校验；patient/drug/medical-record/outpatient/notification/audit 与 ai-service 的业务 internal 端点已改为带 `patient:read`、`patient:write`、`drug:read`、`drug:write`、`medical-record:read`、`doctor:read`、`department:read`、`notification:write`、`audit:write`、`ai:read`、`ai:write`、`file:write` 的服务权限点。遗留：本地签发默认 provider 仍可配置 `*`，服务账号状态只在 auth-service API Key 换发时校验，生产态是否禁用本地默认签发需单独决策。
- `92646d4 fix(auth-service): 补齐内部认证校验端点`：新增 `GET /internal/auth/verify`、`GET /internal/auth/service-verify`、`GET /internal/auth/users/{userId}/roles`。`verify` 只接受用户 JWT；`service-verify` 只接受 SERVICE JWT，并校验 `sys_service_account` 为 ACTIVE 且 token scope 不超账号授权；用户角色查询要求当前请求为有效 SERVICE JWT，并复用 Redis 角色缓存 / PATIENT 兜底。验证：先新增 3 个 MockMvc 红灯测试，因端点不存在落到静态资源处理器并失败；实现后 `mvn -f backend/pom.xml -pl :auth-service -am "-Dtest=AuthFlowTest#internalAuthVerify_acceptsUserTokenAndRejectsServiceToken+internalServiceVerify_acceptsActiveServiceTokenAndRejectsUserOrExcessScope+internalUserRoles_requiresServiceTokenAndReturnsRedisFallbackRole" "-Dsurefire.failIfNoSpecifiedTests=false" test` 通过 3 项。

### 0.2 2026-07-18 最新执行回写

- `b5b37ad fix(mq): 收紧消费重试与死信确认`：AI 消费者失败会进入专用重试 factory，耗尽后投递持久化 DLQ 并等待 publisher confirm；AI/通知消费者共用的 Redis 幂等改为短租约 `PROCESSING` 后落 `DONE`。这只增强现有消费可靠性，未实现审计生产端 AOP/outbox，因此 #2/#8 仍为部分实现。验证：common-mq 14、notification-service 4、ai-service MQ 17，共 35 项通过。
- `ac706bc fix(ai-service): 收紧影像检测归属与复核链路`：公共端改为 `fileIds`，任务只持久化 canonical MinIO 定位符；逐文件授权并校验 patient/record 一致性，限制 8 张与流式大小；患者、提交医生、同 serviceCode 和医院管理员分域读取，复核人从 JWT 派生并读回完整复核结果；新增 additive 迁移及停写、排空、备份、验收步骤。验证：影像目标测试 119/119、前端生产构建通过、规格与质量复审 PASS。
- `e3383a8 feat(frontend): 补齐移动导航与键盘可访问性`：移动端固定侧栏已替换为 Drawer，桌面折叠保留；补 skip link、语义按钮、44px 触控目标、reduced motion、Element Plus 对比度令牌和 Pixel 7/Axe 门禁。验证：E2E 39 通过、1 个桌面限定用例跳过，生产构建通过。
- `f6e2ec2 feat(frontend): 优化响应式基础与患者入口体验`：已加入医疗视觉令牌、移动端登录/注册、患者首页语义按钮、`useResponsive` 和响应式 Playwright；该提交当时仍遗留固定侧栏和工作台/表格响应式欠债，其中主导航问题已由 `e3383a8` 关闭，其他页面仍需按设计基线继续推进。
- `b270739 perf(ai-service): 合并症状规则与语义召回结果`：纯症状输入先走 Mongo 规则召回，数量不足 topK 时继续 Milvus 语义检索补齐并按疾病编码去重，避免规则召回过窄。验证：`./backend/mvnw -q -f backend/pom.xml -pl :ai-service -am "-Dtest=DiseaseSearchServiceTest,MedicationFunctionServiceTest,RagReadinessServiceTest" "-Dsurefire.failIfNoSpecifiedTests=false" test`。
- Milvus 检索补齐 metric 语义和时延预算：`MILVUS_METRIC_TYPE` 默认 `COSINE` 保持现有“越大越好”行为，显式 `L2` 时将 distance 转为 `1/(1+distance)` 再比较 `min-score`；`backend/data` 导入器使用同一变量创建索引，`EUCLIDEAN` 统一规范为 `L2`；`MILVUS_SEARCH_TIMEOUT_SECONDS` 默认 15s，避免单次向量检索拖垮症状问答。验证：`mvn -q -f backend/pom.xml -pl :ai-service -am "-Dtest=MilvusRestClientTest,DiseaseSearchServiceTest,RagReadinessServiceTest" "-Dsurefire.failIfNoSpecifiedTests=false" test`、`mvn -q -f backend/pom.xml -pl :medical-data-milvus-importer "-Dtest=MedicalDataMilvusImporterTest" test`。
- 2026-07-18 AI 治理补齐：RAG 同疾病多字段/多片段证据改为聚合而非丢弃，Mongo/Embedding/Milvus 召回增加总预算超时降级，疾病缓存 key 纳入 schema、查询、字段过滤、Embedding 模型和 Mongo/Milvus 配置维度；但仍缺可随知识内容更新的显式版本。用药 Function Trace 增加 `medication-tool-trace-v1` schema、工具名/状态白名单和入参摘要校验。验证：`mvn -q -f backend\pom.xml -pl :ai-service -am "-Dtest=DiseaseSearchServiceTest,DiseaseCacheServiceTest,MedicationToolCallTraceTest" "-Dsurefire.failIfNoSpecifiedTests=false" test`。
- 2026-07-18 AI 调用日志可观测性补齐：`ai_call_log` 在服务化字段外新增缓存命中、请求/响应/总 Token、预估成本；摘要和用药分析缓存命中仍写调用日志但 Token/成本为 0，日志失败不影响缓存返回；管理端 AI 调用日志表格与移动端卡片展示调用方、缓存、Token、成本、Trace 和 Request。验证：`mvn -q -f backend\pom.xml -pl :ai-service -am "-Dtest=AiCallLogServiceTest,SummaryServiceCacheLogTest,MedicationAnalysisCacheLogTest,MedicationAnalysisServiceControlledResultTest,MedicationAnalysisCacheKeyTest" "-Dsurefire.failIfNoSpecifiedTests=false" test`、`responsive-table.spec.ts -g "admin AI call log switches to cards on mobile"`、`npm --prefix frontend run build`。

---

## 一、5 项遗留问题清单与现状证据（2026-07-16 历史快照）

| # | 问题 | docs 归属 | 现状 | 一句话结论 |
| --- | --- | --- | --- | --- |
| 1 | RBAC 五表未建 | 修改建议 §2.3（P0） | 完全缺失（仅 sys_user，角色走 Redis，scope 写死 `*`） | 五表只建了 1 张，权限体系未落地 |
| 2 | 症状规则表空有 DDL 未接库 | 修改建议 §3.1（P0） | 已修复运行链路（DDL 齐，已补 Entity/Mapper，RiskRuleEngine 由 Spring 注入并优先读规则快照） | 原问题已关闭；前端证据展示已在问题 5 闭环 |
| 3 | id_no 明文存储 | 修改建议 §5.3（P1） | 未实现（字段/列/唯一性 eq/模糊 like/种子全明文，common-crypto 模块不存在） | 全链路明文，加密基础设施缺失 |
| 4 | 退费流程缺失 | 修改建议 §6.2（P2） | 2026-07-17 已修复挂号费退款最小闭环（refund_order + RefundService/Controller + 患者端入口 + E2E）；历史问题为无表、无 Service、无 Controller、无前端入口 | 挂号费退款闭环已补；真实支付渠道仍需后续接入 |
| 5 | 审计/通知 MQ 链路空转 | 修改建议 §2.2/§4.2（P0） | 部分实现（消费端 AuditLogConsumer 完整 + audit_log 表 + 幂等；生产端 @AuditLog AOP/Producer 完全缺失，5 个业务服务只有 ai-service 引了 common-mq 且只发 AI 日志） | 消费端可用、生产端空转，队列永远没消息 |

### 1.1 问题 1：RBAC 五表未建

| 检查项 | 现状 | 证据 |
| --- | --- | --- |
| sys_role 等 4 张 RBAC 表 | 完全缺失 | `backend/auth-service/src/main/resources/schema.sql` 仅有 `sys_user`、`sys_service_account`、`login_log` 三表 |
| scope 计算 | 硬编码全权限 | `AuthServiceImpl.java:222` `List<String> scope = List.of("*"); // 冒烟期全权限；RBAC 阶段改为查 sys_role_permission` |
| 角色来源 | Redis 兜底 PATIENT | `AuthServiceImpl.java:214-221` 读 Redis 失败兜底 `PATIENT`；`DataSeeder.java:90` 种子角色也写 Redis（TTL 365 天） |
| 列表查询 | 内存分页退化为内存过滤 | `AuthServiceImpl.java:388-421` 注释「RBAC 五表落地后恢复 selectPage」 |

### 1.2 问题 2：症状规则表空有 DDL 未接库

| 检查项 | 现状 | 证据 |
| --- | --- | --- |
| 3 张规则表 DDL | 已实现 | `schema-ai.sql:198-226` symptom_rule / high_risk_symptom_rule / negative_rule 均有定义 |
| 规则来源 | DB 规则快照优先，硬编码降级 | `RiskRuleEngine.java` 首次评估加载启用规则；规则库为空或刷新失败时保留硬编码 fallback |
| 实例化方式 | Spring 注入 | `RiskRuleEngine` 标记 `@Component`，`SymptomChatService` 与 `TriageService` 均构造器注入 |
| Entity / Mapper | 已实现 | `SymptomRuleEntity` / `HighRiskSymptomRuleEntity` / `NegativeRuleEntity` 及 3 个 Mapper 已映射规则表 |

### 1.3 问题 3：id_no 明文存储

| 检查项 | 现状 | 证据 |
| --- | --- | --- |
| Patient.idNo 字段 | 明文，带 TODO | `Patient.java:41-42` `/** 证件号（TODO AES-256 加密，见 schema.sql 注释） */` |
| schema 列定义 | 明文 VARCHAR | `schema.sql:15-18` 注释「先明文存储」 |
| 唯一性校验 | 明文精确匹配 | `PatientServiceImpl.java:79` `.eq("id_no", req.getIdNo())`；`:301` `.eq("id_no", idNo)` |
| 模糊查询 | 明文 like | `PatientServiceImpl.java:155` `.or().like("id_no", keyword)` |
| 种子数据 | 明文真实格式 | `schema.sql:41` `id_no='110101199005201234'` |
| common-crypto 模块 | 完全缺失 | `backend/medconsult-common/` 下无 common-crypto，common-security 仅含 JwtCodec |

### 1.4 问题 4：退费流程缺失

> 2026-07-17 更新：本问题的挂号费退款最小闭环已补齐；真实支付渠道、回调、对账和失败重试仍属于后续支付产品化工作。

| 检查项 | 现状 | 证据 |
| --- | --- | --- |
| paymentStatus 白名单 | 已修复：通用 payment patch 只允许普通支付状态，退款状态由退款流程写入 | `AppointmentServiceImpl.updatePayment` 拒绝 REFUNDING/REFUNDED |
| 取消已付订单 | 保持只取消预约，不再承诺自动退款 | 退款改为患者单独点击“申请退款”触发 |
| refund_order 表 | 已实现 | `backend/outpatient-service/src/main/resources/schema.sql` 新增 `refund_order` |
| RefundService/Controller | 已实现 | `backend/outpatient-service/src/main/java/com/medconsult/outpatient/refund/` |
| 前端入口 | 已实现 | `frontend/src/api/appointment.js`、`frontend/src/views/patient/appointment/MyAppointment.vue`、`frontend/e2e/refund.spec.ts` |

### 1.5 问题 5：审计/通知 MQ 链路空转

| 检查项 | 现状 | 证据 |
| --- | --- | --- |
| AuditLogConsumer | 已实现 | `AuditLogConsumer.java:39-76` 监听队列 + SETNX 幂等（72h）+ 写 audit_log |
| AuditLogEvent / @AuditLog / Producer | 已接入 | `common-mq` 已提供共享 `AuditLogEvent`、`@AuditLog` AOP 和 `AuditLogProducer`，生产端写入各服务本地 `local_message` |
| common-mq 依赖 | 已接入 | ai/notification/patient/outpatient/auth/medical-record/drug 已引入；影像任务提交已走事务 `local_message` outbox |
| 谁实际发 RK_AUDIT_LOG / 业务 MQ | 已接入 | patient-service 患者建档/更新/状态变更、outpatient-service 预约创建/支付/签到/状态变更/取消、auth-service 登录成功/改密/补建档绑定、medical-record-service 病历/附件/处方关键写操作、drug-service 药品创建/库存入库/出库/回滚补偿已写 `audit.log` outbox；ai-service 影像任务提交已写 `ai.image.detect` outbox |
| local_message 表 | 已实现 | `LocalMessage.java:27` + `LocalMessageMapper.java` + MessageDispatcher 扫描投递机制齐全 |

---

## 二、为什么前期 backend 实现时没考虑到、多轮 review 没发现

### 2.1 分类框架（4 类）

| 类别 | 含义 | 责任归属 |
| --- | --- | --- |
| **A 有意降级** | 代码有明确 TODO/注释说明「先 X，后续改 Y」，是有意识的技术债 | 可接受 |
| **B 半成品** | DDL/枚举/接口契约已建，但实现层未跟进，且**无 TODO 标记** | 流程缺陷 |
| **C 纯遗漏** | 文档、契约、代码三处都没提 | 严重 |
| **D 文档已列但 review 未对照** | `修改建议.md` 列了，但 5 轮 review 没把它当 checklist 逐条核对 | 流程缺陷 |

### 2.2 5 项归类

| # | A 有意降级 | B 半成品 | C 纯遗漏 | D 文档已列未对照 |
| --- | --- | --- | --- | --- |
| 1 RBAC | ✅（`scope = List.of("*")` 冒烟期标注 + DataSeeder Redis 注释说明） | — | — | ✅ |
| 2 症状规则表 | ✅（已接 Entity/Mapper + 规则快照，保留硬编码 fallback） | — | — | ✅ |
| 3 id_no | ✅（字段 + schema 双 TODO，明确等 common 加密模块） | — | — | ✅ |
| 4 退费 | ✅（docs 自分类 P2，docs 已声明「后续迭代」） | — | — | ✅ |
| 5 审计 MQ | — | ✅（消费端完整 + 生产端 AuditLogEvent 注释「未来」） | — | ✅ |

**关键发现：C 类（纯遗漏）为 0。** 没有一项是"彻底被忘掉"——全部要么是有意识降级（A），要么是半成品欠债（B），且全部在 `修改建议.md` 列出但 review 未逐条对照（D）。

### 2.3 根因：三条 review 基线的天然盲区

fullstack-review 的三条核查基线，对"架构半成品"类问题都有盲点：

| 基线 | 能发现 | 对 5 项的盲区 |
| --- | --- | --- |
| 前端调用 → 后端 API 契约 | 接口字段失配、空列、假成功占位 | 5 项都不影响"现有接口能跑通"，前端不调 RBAC 表也不调 audit producer |
| docs 契约 → 实现一致性 | 文档明示但代码缺失的功能 | 5 项在 docs 里要么标 P2（退费）要么以 TODO 形式存在，docs 没把它们写成"硬性要求" |
| git diff 改动核查 | 本次提交引入的回归 | 5 项都是"长期欠债"，不在任何一次 diff 里"突然出现" |

### 2.4 核心流程缺陷：修改建议.md 从未被当 checklist 使用

`修改建议.md` 是一份现成的 14 条问题总览表，但 fullstack-review 的 SKILL.md 第 0 步「扫描 docs/ 基线」在 1–4 轮被简化为"看需求文档 + 接口文档"，没有逐条核对修改建议表。**这是最高杠杆的流程改进点。**

### 2.5 各项合理性评估

| # | 前期未做的合理性 | 是否应现在补 |
| --- | --- | --- |
| 1 RBAC | 合理（冒烟期 `*` 兜底是标准降级） | 是（用户已确认 DB 优先 + `*` 兜底，灰度切换不引发 403 回归） |
| 2 症状规则表 | 不合理（既然写了 DDL，应至少给 Entity/Mapper + 硬编码 fallback） | 是（接库但保留硬编码 fallback，零行为漂移） |
| 3 id_no | 合理（等 common 加密模块） | **是（TODO 自己说"待 common 加密模块就绪"，现在就是那个时点）** |
| 4 退费 | 合理（P2，非核心闭环） | 是（用户确认做；前端已误报"原路退回"构成对客误导，必须修） |
| 5 审计 MQ | 不合理（消费端都做了，生产端空着等于功能 0%） | 是（根因是生产端完全空白，必须可靠投递） |

---

## 三、目标实施策略（用户已确认范围）

| # | 范围决策 | 实施要点 |
| --- | --- | --- |
| 1 RBAC | **DB 优先 + `*` 兜底** | 新建 4 表 + Entity/Mapper；AuthServiceImpl 查 sys_role_permission；**用户无权限记录时兜底 scope=`*`**（避免 403 回归）；DataSeeder 补 sys_user_role；PermissionAspect 不变 |
| 2 症状规则表 | **接库 + 硬编码 fallback** | 3 Entity + 3 Mapper；RiskRuleEngine 重构为查库 + 硬编码降级；**库为空时行为与现状完全一致**；SymptomChatService 改注入 |
| 3 id_no | **完整做** | 新建 common-crypto 模块（AES-256-GCM + 密钥从 Nacos）+ MyBatis TypeHandler；新增 `id_no_hash` 列支撑唯一性/模糊检索；数据迁移脚本把存量明文加密 |
| 4 退费 | **完整流程** | refund_order 表 + `POST /appointments/{id}/refund` + RefundRequest/RefundResponse DTO + RefundService（Redis 锁防重复退款）+ 前端入口 + 修正"原路退回"误导文案 |
| 5 审计 MQ | **可靠投递** | AuditLogEvent 移到 common-mq；关键写服务逐步加 common-mq 依赖；每服务 local_message 表；生产端先写本地 outbox，再由 MessageDispatcher 可靠投递 |

### 3.1 横切原则（所有 5 项必须遵守）

1. **不留无标记半成品**：任何"先 X 后 Y"的实现必须在代码注释里写明状态（避免问题 2/5 那种 DDL 在、逻辑不在、还没 TODO 的代码卫生问题）。
2. **降级路径零行为漂移**：RBAC 的 `*` 兜底、规则表的硬编码 fallback，上线后行为必须与现状逐字节一致，再逐步收紧。
3. **数据迁移可重入**：id_no 加密的迁移脚本必须可重复执行（用 hash 列是否已填充判断）。
4. **每项独立 commit**：5 项各自内聚，不混合提交；commit message 用 Conventional Commits 中文描述。
5. **仅 commit 不 push**：严格遵守 AGENTS.md，未经用户明确指示不 push。

---

## 四、分批实施顺序

| 批次 | 内容 | 风险 | 依据 |
| --- | --- | --- | --- |
| **批次 1（并行，基础设施）** | RBAC 五表 + 审计 MQ 接业务 | 中 | 都是横切基础设施，互不依赖，可并行；RBAC 是后续权限标注的前置 |
| **批次 2（单服务闭环）** | 症状规则接库 + 退费流程 | 中 | 都是单服务内闭环，改动局部 |
| **批次 3（最高风险，需迁移）** | id_no AES-256 加密 | 高 | 涉及新建模块 + TypeHandler + hash 列 + 存量数据迁移，必须单独批次充分验证 |

详细任务分解见后续工作计划文档（`docs/遗留问题实施计划.md`）。

---

## 五、Round 8 review 记录（分支 fix/feat-merge-and-backend，2026-07-16）

本轮工作：合并 `feat-front-large-edit` 前端 + 实现后端修改.md 的 6 类接口 + fullstack-review。

### 5.1 修改建议.md 当时 14 条总览核对（review 第 0 步，历史快照）

本轮**未推进** P0/P1 架构遗留项（RBAC 批次 2-12、审计 MQ 生产端、id_no 加密、处方表等），状态与上轮一致，沿用本文件§一总览表。本轮聚焦前后端对接，当时 14 条总览状态无变化。当前 15 项状态以本文§零和 `docs/修改建议.md` 第一节为准。

### 5.2 后端修改.md 6 条处理状态

| # | 需求 | 状态 | commit |
|---|------|------|--------|
| 7 | 预约列表 patientName | ✅ 已实现（核查后端 AppointmentDTO.ListItem 含 patientName，Feign 回填），关闭 | — |
| 15 | 科室增改删 | ✅ 已实现 POST/PATCH/DELETE /departments，departmentNo 后端生成，删前校验引用 | 80e71aa |
| 16 | 默认排班 | ⏸ 未实现（需先设计默认排班模型，本轮不做） | — |
| 17 | 排班全量更新 + ListItem 补字段 | ✅ 已实现 PUT /schedules/{id}，ListItem 补 doctorId/departmentId | 22d266a |
| 19 | 修改密码 | ✅ 已实现 POST /auth/change-password | d430af3 |
| 20/21 | 管理员创建用户 | ✅ 已实现 POST /auth/users（含管理类角色 + Redis 角色 key） | fed8b31 |
| — | 全局库存流水（drug.js 新增） | ✅ 已实现 GET /drugs/stock/flows | 891649c |

### 5.3 Round7 安全修复回归（合并后仍存在，grep 证据）

- REFUNDING 映射：MyAppointment.vue(L110 PAYMENT_STATUS)、ReceptionList.vue(L189 REFUNDING) ✓
- AI 日志分页：AiCallLog.vue(el-pagination) ✓
- IDOR：AiController.java(L151 @Permission ai:summary:confirm roles=DOCTOR/HOSPITAL_ADMIN) ✓

### 5.4 review 发现并修复的 5 个前后端对接问题（commit c132b05）

| Fix | 级别 | 问题 | 处置 |
|-----|------|------|------|
| 1 | HIGH 安全 | drug-service 全局流水缺鉴权 | 补 @Permission(PHARMACY_ADMIN/HOSPITAL_ADMIN) |
| 2 | HIGH 契约 | 排班 STOPPED≠后端 SUSPENDED，停诊必 400 | 前端统一 SUSPENDED |
| 3 | MED 契约 | 排班编辑医生字段后端忽略 | 编辑模式 :disabled |
| 4 | MED 契约 | 管理员建号 status 后端硬编码 ACTIVE | 去误导性禁用选项 |
| 5 | LOW 契约 | mapStockFlow 字段映射错位 | 对齐 stockFlowId/drugNo |

### 5.5 遗留待办（本轮范围外，记录留痕）

- 假占位功能后端接口待补：医生增改删、用户删除（前端已改为明确拒绝，非假成功）
- drug-service 其他写接口（create/inbound/outbound）类级鉴权缺失（既有问题，本轮仅补全局流水）
- change-password 缺频率限制 + 审计日志（防爆破）
- 预约列表 patientName 在 feat 分叉旧后端确为缺失，当前 HEAD 已实现（#7 关闭依据）
